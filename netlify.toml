[build]
  command = "npm run build"
  publish = "dist"

# Optional: pin Node version for Netlify build environment
[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
